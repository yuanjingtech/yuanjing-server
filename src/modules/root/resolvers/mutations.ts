import {addGroup} from "../services/index";

export const mutations = {
    // addGroup: async (root: any, args: any, context: any) => {
    //     return addGroup(args.input);
    // },

    test: async (root: any, args: any, context: any) => {
        return null
    },
};
